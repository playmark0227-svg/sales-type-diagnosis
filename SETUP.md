# 受検者の記録を有効にする手順

診断結果を Google スプレッドシートに自動で貯める設定です。**15分ほど**で終わります。
プログラミングの知識は要りません。コピペだけです。

設定するまでは、受検者情報の入力画面は出ず、診断ツールとしてだけ動きます。

---

## 全体像

```
受検者のブラウザ  →  Apps Script（受け取り係）  →  スプレッドシート
```

サーバーを借りる必要はありません。全部 Google の無料の仕組みで動きます。

---

## 1. スプレッドシートを作る

1. Googleドライブで **新規 → Google スプレッドシート** を作成
2. 名前を「**営業タイプ診断 記録**」などに変更

シートのタブは作らなくて大丈夫です。最初の受信時に「診断結果」「生回答」の2つが自動で作られます。

---

## 2. Apps Script を貼る

1. そのスプレッドシートで **拡張機能 → Apps Script** を開く
2. 最初から入っている `function myFunction() {}` を**全部消す**
3. このリポジトリの [`tools/apps-script/Code.gs`](tools/apps-script/Code.gs) の中身を**全部コピーして貼り付け**
4. 上のほうにある次の行を探して、合い言葉を決めて書き換える

   ```js
   var PASSCODE = 'CHANGE_ME';
   ```

   例）`var PASSCODE = 'insup2026';`

   > **合い言葉はここでしか書き換えないでください。**
   > GitHub にあるファイルのほうに書くと、公開リポジトリなので誰でも読めてしまいます。

5. **保存**（フロッピーのアイコン）

---

## 3. ウェブアプリとして公開する

1. 右上の **デプロイ → 新しいデプロイ**
2. 歯車マーク → **ウェブアプリ** を選ぶ
3. 設定を次のようにする

   | 項目 | 選ぶもの |
   |---|---|
   | 説明 | 営業タイプ診断（なんでも可） |
   | 次のユーザーとして実行 | **自分** |
   | アクセスできるユーザー | **全員** |

   > 「全員」にしないと受検者から送信できません。
   > 誰でも**書き込める**という意味ではありません。合い言葉が合わないものは弾かれますし、
   > **スプレッドシートの中身は誰にも見えません。**

4. **デプロイ** を押す
5. 初回は「このアプリは Google で確認されていません」と出ます。
   **詳細 → （プロジェクト名）に移動 → 許可** と進んでください。自分で書いたスクリプトなので問題ありません。
6. 表示された **ウェブアプリのURL** をコピー
   （`https://script.google.com/macros/s/............/exec` という形）

### 動作確認

コピーしたURLをブラウザのアドレスバーに貼って開いてみてください。

```json
{"ok":true,"message":"営業タイプ診断の受信スクリプトは動作しています。"}
```

これが出れば成功です。

---

## 4. 診断ツール側にURLを貼る

[`js/config.js`](js/config.js) を開いて、`endpoint` にURLを貼ります。

```js
window.CONFIG = {
  endpoint: 'https://script.google.com/macros/s/............/exec',
  ...
};
```

保存して GitHub に push すれば、数分後に公開ページへ反映されます。

これで、診断をはじめる前に受検者情報の入力画面が出るようになります。

---

## 5. 配布する

合い言葉を伝えた上で、URLを渡してください。
URLの後ろにタグを付けると、**どこ経由の受検か**がシートに記録されます。

| 用途 | 渡すURL |
|---|---|
| 採用の応募者 | `.../sales-type-diagnosis/?src=saiyo` |
| 社内メンバー | `.../sales-type-diagnosis/?src=shanai` |
| SNS・イベント | `.../sales-type-diagnosis/?src=sns` |

タグの文字列は自由です。何も付けなければ `direct` として記録されます。

---

## 記録される内容

### シート「診断結果」（1人1行・45列）

受検ID / 受検日時 / 用途タグ / 氏名 / メールアドレス / 会社・組織 / 部署・職種 / 営業経験 /
版 / 所要（分） / タイプコード / タイプ名 / 4軸それぞれの文字と% / 総合スコア /
8スキルの点数 / 5職種の適性 / 推奨職種 / 強みTOP3 / 伸びしろTOP3 / 営業特性★8項目

そのまま並べ替え・フィルタ・ピボットが使えます。

### シート「生回答」（1人1行・69列）

受検ID / 受検日時 / 氏名 / メールアドレス / 版 / T01〜T40 / S01〜S24

設問ごとの1〜5がそのまま入ります。設問の精度検証や、あとから採点方法を変えて再集計したいときに使います。

---

## よくあるつまずき

**「合い言葉が違います」と出る**
Apps Script 側の `PASSCODE` と、受検者が入れた文字列が一致していません。
全角スペースや、コピー時に混ざった空白に注意してください。

**「記録先に接続できませんでした」と出る**
デプロイの「アクセスできるユーザー」が **全員** になっているか確認してください。
また、Code.gs を書き換えたあとは **デプロイ → デプロイを管理 → 編集（鉛筆）→ バージョン「新バージョン」→ デプロイ** をしないと反映されません。

**シートに何も増えない**
Apps Script の左側「実行数」でエラーを確認できます。
URLの末尾が `/exec` になっているか（`/dev` ではないか）も見てください。

**同じ人が2行入っている**
受検IDが同じものは無視されるようになっているので、通常は起きません。
同じ人が最初から2回受検した場合は、受検IDが変わるため2行になります（これは正しい動作です）。

---

## 記録をやめたいとき

`js/config.js` の `endpoint` を空にすれば、入力画面も送信もなくなります。

```js
endpoint: '',
```

---

## 個人情報について

- 受検者には入力画面で、**何を・誰が・何のために**受け取るかを明示し、同意のチェックを必須にしています。
- データは**ご自身のGoogleアカウント内**にのみ保存されます。第三者のサービスは経由しません。
- 通信は https で暗号化されています。
- 採用で使う場合、応募者への利用目的の通知は募集要項や案内メールにも記載しておくと安全です。
- 不要になった行は、シートから削除すれば消えます。
