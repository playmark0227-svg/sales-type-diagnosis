# 営業タイプ診断

営業の「タイプ（スタイル）」と「スキル（実力）」を**分けて**測る、ブラウザだけで動く診断ツールです。
サーバー不要・ビルド不要。GitHub Pages にそのまま置けます。

---

## 何が出るか

| セクション | 内容 |
|---|---|
| SALES TYPE | 4文字コード（例：HEPS）＋タイプ名＋キャッチコピー |
| 4軸バランス | H/F・L/E・P/D・S/C を 0〜100% で表示。「ほぼ互角」も明示 |
| 営業特性 | 新規開拓・関係構築・ヒアリング・提案力・クロージング・行動量・戦略性・顧客管理を★1〜5 |
| 強み / 気をつけたいクセ | タイプ別に 5件 / 4件 |
| SALES SKILL | 8スキルを 15〜95点＋レーダーチャート＋総合スコア |
| TOP3 / 伸びしろTOP3 | スキルスコアから自動抽出 |
| 職種適性 | IS / FS / CS / AM / 営業マネージャー を 0〜100 |
| 向いている営業 / 苦手になりやすい仕事 | ◎4件・○3件・▲4件 |
| このタイプの育て方 | マネージャー向け。効果的4件・逆効果4件・配属直後1ヶ月の指示 |
| 成長アドバイス | タイプ由来3件＋スコアが低い3スキルの具体アクション各3件 |
| 相性 | 組むとうまくいくタイプ2件・摩擦が起きやすいタイプ1件 |

---

## 4つの軸

| 軸 | 前者 | 後者 | 何を見ているか |
|---|---|---|---|
| ① | **H** Hunter：新規開拓型 | **F** Farmer：既存深耕型 | どこに向かうか |
| ② | **L** Logic：論理型 | **E** Emotion：共感型 | 何で相手を動かすか |
| ③ | **P** Push：主導型 | **D** Discover：探索型 | どう商談を進めるか |
| ④ | **S** Speed：即行動型 | **C** Careful：慎重型 | どう意思決定するか |

4文字の組み合わせで 16タイプ。

---

## 設問と採点

### 出題

- **フル版 64問**（タイプ40問＋スキル24問／約8分）
- **ライト版 36問**（タイプ20問＋スキル16問／約4分）

すべて5段階（1 = 全く当てはまらない 〜 5 = 非常に当てはまる）。

> ライト版はフル版の設問から `lite: true` のものだけを出します。
> スキルは8項目あるため、各項目に最低2問を確保して16問にしています（12問だと測れない項目が出るため）。

### タイプ判定

各設問には軸（`axis`）と向き（`dir`）があります。

```
寄与  = dir × (回答 − 3)          →  −2 〜 +2
軸合計 = Σ寄与                     →  −2n 〜 +2n
％    = (軸合計 + 2n) ÷ 4n × 100  →  0 〜 100（100 = 完全に前者側）
```

- 50% 以上なら前者の文字（H / L / P / S）、未満なら後者（F / E / D / C）。
- 各軸10問のうち **5問は逆転項目**（`dir: -1`）です。「なんでも "当てはまる" を選ぶ癖」を打ち消すため。
- 50% に近い軸は「ほぼ互角です」と表示し、隣のタイプも読むよう案内します。

### スキル採点

難易度（`level`）で重みをつけます。できる人しか5を付けない設問を重く見るためです。

```
重み   : level1 = 1.00 / level2 = 1.15 / level3 = 1.32
正規化 : (Σ重み×回答 − Σ重み) ÷ (Σ重み × 4)   → 0〜1
スコア : 15 + 正規化 × 80                      → 15〜95
```

0点や100点を出さないのは、診断体験として意味がないからです。

### 職種適性

```
適性 = スキル適合 × 0.7 + タイプ適合 × 0.3
```

**スキルを主・タイプを従**にしています。そうしないと「タイプが合っているだけで適性が高い」と誤読されるためです。
実力がつけば数字が上がる設計になっています。

### 営業特性★

4軸の連続値から計算します（`js/scoring.js` の `TRAIT_DEFS`）。
スキルスコアは一切混ぜていません。**タイプ ≠ 営業能力**という前提を崩さないためです。

---

## ファイル構成

```
index.html                  画面（5スクリーン：スタート / 受検者情報 / 設問 / 中間 / 結果）
css/style.css               スタイル。デザイントークンは冒頭の :root
js/config.js                記録先の設定。ここにApps ScriptのURLを貼る
js/scoring.js               採点ロジック（軸・スキル・特性★・職種適性）
js/app.js                   画面制御・結果描画・記録の送信・背景パーティクル
js/data.js                  ★自動生成★ 直接編集しないこと
content/*.json              文章と設問の原本。ここを編集する
tools/build-data.js         content/*.json → js/data.js の変換と検証
tools/apps-script/Code.gs   スプレッドシート側の受信スクリプト
assets/*.png                ロゴとファビコン
```

---

## 受検者の記録

`js/config.js` の `endpoint` に Google Apps Script のURLを貼ると、
診断の前に受検者情報の入力画面が出て、結果がスプレッドシートに自動で貯まります。

**設定手順は [SETUP.md](SETUP.md) にまとめてあります。**

| | |
|---|---|
| 保存先 | ご自身のGoogleドライブ内のスプレッドシート（第三者サービスを経由しません） |
| 入力項目 | 氏名・メール（必須）／会社・部署・営業経験年数（任意）／合い言葉（必須）／同意チェック（必須） |
| 記録内容 | 上記＋タイプ・4軸%・スキル8項目・総合・職種適性5つ・★8項目・全64問の生回答・所要時間 |
| いたずら対策 | 合い言葉をサーバー側で照合。合わないものは弾かれます |
| 経路の識別 | URLに `?src=saiyo` のようなタグを付けて配布すると、シート上で区別できます |

`endpoint` が空のあいだは入力画面も送信も発生せず、診断ツールとしてだけ動きます。

### 通信まわりの実装メモ

Apps Script のウェブアプリは `OPTIONS` を扱えないため、プリフライトが起きない
`Content-Type: text/plain` で POST しています。応答は 302 の先（`googleusercontent.com`）が
`Access-Control-Allow-Origin: *` を返すので読み取れます。

読み取れない環境（社内プロキシなど）に備えて、二段の逃げ道を用意しています。

- **合い言葉の確認** … POST が失敗したら JSONP（`<script>` 読み込み）に切り替え。CORSの影響を受けません
- **結果の送信** … POST が失敗したら `mode: 'no-cors'` で投げ直し。届きますが受信確認はできません

`no-cors` で投げ直すと、実は最初のPOSTも届いていて二重になることがあります。
そのため **受検IDが同じ行はスプレッドシート側で無視** するようにしてあります（`hasSid`）。
再送ボタンを何度押しても行は増えません。

### ロゴ素材

| ファイル | 内容 | サイズ |
|---|---|---|
| `assets/logo-mark.png` | マークのみ・白の透過PNG | 246×264 |
| `assets/logo-word.png` | ワードマーク「Insup」・白の透過PNG | 419×172 |
| `assets/logo-lockup.png` | マーク＋ワードマークの縦組み | 423×467 |
| `assets/favicon.png` | 黒地＋白マーク | 180×180 |
| `assets/apple-touch-icon.png` | 同上 | 512×512 |

コーポレートサイトの `images/logo.jpg`（白いロゴを暗い壁に取り付けた写真）から、
輝度をアルファに変換して切り出したものです。輝度分布が 0.00〜0.31 と 0.78〜1.00 に
きれいに分かれていたため、`smoothstep(0.34, 0.70)` でアンチエイリアスを保ったまま抜けました。

**白抜きなので、白い背景で使うときは `filter: invert(1)` が必要です。**
印刷用CSSではその指定を入れてあります。

### 文章を直したいとき

`content/` の JSON を編集して、下記を実行してください。

```bash
node tools/build-data.js
```

構造チェック（16タイプ揃っているか、件数、相性コードが実在するか、軸ごとの問数、ID重複 など）が走り、
問題があれば行を指摘して止まります。

| ファイル | 中身 |
|---|---|
| `content/types-HL.json` | HLPS / HLPC / HLDS / HLDC |
| `content/types-HE.json` | HEPS / HEPC / HEDS / HEDC |
| `content/types-FL.json` | FLPS / FLPC / FLDS / FLDC |
| `content/types-FE.json` | FEPS / FEPC / FEDS / FEDC |
| `content/questions-type.json` | タイプ判定40問 |
| `content/questions-skill.json` | スキル24問 |
| `content/advice.json` | 8スキルの解説・アクション、5職種の定義 |

### デザイン

株式会社Insup コーポレートサイト（insup.co.jp）のデザインシステムに準拠しています。

| | |
|---|---|
| 背景 | 純黒 `#000`。面は `rgba(255,255,255,.02)` |
| 文字 | `#fff` / `#999` / `#666` / `#444` |
| 罫線 | `1px solid rgba(255,255,255,.1)` |
| 角丸 | **0**（全要素） |
| 色アクセント | **なし**（完全モノクローム） |
| ボタン | `2px solid #fff`・角丸0・`15px 40px`。主＝白地に黒文字、副＝透明に白文字 |
| 見出し | 下に `60×2px` の白バー（`::after`） |
| 書体 | 和文 Noto Sans JP / 欧文・数字 Montserrat（Google Fonts） |
| 装飾 | 最初の画面だけ、コーポレートサイトと同じ点と線のパーティクル |

トークンは `css/style.css` 冒頭の `:root` にまとまっています。
パーティクルは `js/app.js` の `bg` モジュール（`prefers-reduced-motion` を尊重し、設問・結果画面では停止）。

---

## ローカルで確認する

```bash
python3 -m http.server 8000
```

`http://localhost:8000` を開いてください。
（`file://` で直接開いても動きますが、ブラウザによっては読み込みに失敗します）

---

## 使うときの注意

- 回答はブラウザ内だけで処理され、サーバーには送信されません。途中の回答は `localStorage` に保存され、再訪時に再開できます。
- 自己申告にもとづく行動特性の可視化ツールです。医学的・心理臨床的な診断ではありません。
- **採用の合否や人事評価を単独で決めるために使わないでください。** 面談やアセスメントと組み合わせ、配置・育成の共通言語として使うのが本来の用途です。
